Процедура ПриЗаписи(Отказ, РежимЗаписи)

	// Записываем историю изменения статуса клиента
	НаборЗаписей = РегистрыСведений.ИсторияСтатусовКлиентов.СоздатьНаборЗаписей();

    Запись = НаборЗаписей.Добавить(); 
    Запись.Период = ТекущаяДата();
    Запись.Контрагент = Ссылка;
    Запись.Статус = Статус;

    НаборЗаписей.Записать(Ложь);

КонецПроцедуры
